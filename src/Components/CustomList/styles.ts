import {StyleSheet} from 'react-native';
import {dimensions} from '../../Constants/utility';

export const styles = StyleSheet.create({
  view: {
    paddingBottom: 30,
  },
  flatList: {
    paddingBottom: dimensions.height * 0.078,
  },
});
